# Guide CLI Meshtastic (MultiOS) — version sanitizée

Prérequis :
- Firmware Meshtastic installé (https://flasher.meshtastic.org)
- Python + meshtastic-cli

Commandes utiles :

🔁 Changer le nom du module :
  meshtastic --port COMx --set ownerlong "Nom_Code"

🔐 Changer le canal et référencer la clé (PSK_REF) :
  meshtastic --port COMx --ch-set name "FAMILIA"
  meshtastic --port COMx --ch-set pskref ${MESH_PSK_REF_FAMILIA}

🌐 Configurer le Wi-Fi (exemple placeholder) :
  meshtastic --port COMx --set wifi_ssid "<WIFI_SSID>"
  meshtastic --port COMx --set wifi_password "<WIFI_PASSWORD>"

📡 Configurer MQTT (exemple placeholder) :
  meshtastic --port COMx --set mqtt_address "<MQTT_HOST>"
  meshtastic --port COMx --set mqtt_username "${MQTT_USERNAME}"
  meshtastic --port COMx --set mqtt_password "${MQTT_PASSWORD}"

🛰️ Configurer la position GPS fixe (optionnel) :
  meshtastic --port COMx --setlat <LATITUDE>
  meshtastic --port COMx --setlon <LONGITUDE>
  meshtastic --port COMx --setalt <ALTITUDE_M>

✅ TEST :
  meshtastic --port COMx --info

💬 Envoyer un message :
  meshtastic --port COMx --sendtext "Coucou la team"
