# 🧒 Config Meshtastic - Module ENFANT (TRACEUR SECURISÉ)

channel_url: <CHANGE_ME_CHANNEL_URL>
channel_settings:
  - name: FAMILIA
    id: 0
    psk_ref: ${MESH_PSK_REF_FAMILIA}
  - name: CASTILLO
    id: 3
    psk_ref: ${MESH_PSK_REF_CLAN}
  - name: PERRITOS
    id: 4
    psk_ref: ${MESH_PSK_REF_CLAN}

config:
  bluetooth:
    mode: DISABLED
  lora:
    region: EU_868
    channelNum: 4
    txPower: 20
    hopLimit: 5
  position:
    gpsMode: ENABLED
    positionBroadcastSecs: 120
  power:
    isPowerSaving: true

owner: KIDDO
owner_short: KID
# 🤖 Aucun secret n’est embarqué en clair. Utiliser les références.
