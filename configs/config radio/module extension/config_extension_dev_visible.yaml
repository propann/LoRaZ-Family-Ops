# 📡 Config Meshtastic - Module EXTENSION (répéteur)

channel_url: <CHANGE_ME_CHANNEL_URL>
channel_settings:
  - name: FAMILIA
    id: 0
    psk_ref: ${MESH_PSK_REF_FAMILIA}
  - name: EXPLORA
    id: 2
    psk_ref: ${MESH_PSK_REF_CLAN}
  - name: CASTILLO
    id: 3
    psk_ref: ${MESH_PSK_REF_CLAN}

config:
  bluetooth:
    mode: DISABLED
  lora:
    region: EU_868
    channelNum: 2
    txPower: 23
    hopLimit: 7
  network:
    wifiEnabled: false
  position:
    gpsMode: DISABLED
  power:
    isPowerSaving: false

owner: RELAY
owner_short: EXT
# 🚧 Secrets gérés via PSK_REF.
