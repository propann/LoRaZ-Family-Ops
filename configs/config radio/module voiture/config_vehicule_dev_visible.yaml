# 🚗 Config Meshtastic - Module VEHICULE (ROVER)
# Profil mobile longue portée

channel_url: <CHANGE_ME_CHANNEL_URL>
channel_settings:
  - name: FAMILIA
    id: 0
    psk_ref: ${MESH_PSK_REF_FAMILIA}
  - name: TESORO
    id: 6
    psk_ref: ${MESH_PSK_REF_ALERTA}
  - name: ALERTA_ROJA
    id: 5
    psk_ref: ${MESH_PSK_REF_ALERTA}

config:
  bluetooth:
    mode: DISABLED
  lora:
    region: EU_868
    channelNum: 6
    txPower: 23
    usePreset: true
    hopLimit: 7
  network:
    wifiEnabled: true
    ntpServer: pool.ntp.org
  position:
    gpsMode: ENABLED
    positionBroadcastSecs: 30
  power:
    isPowerSaving: false
  security:
    serialEnabled: true

owner: ROVER
owner_short: SUV
# ⚙️ Les clés sont référencées, jamais stockées en clair.
